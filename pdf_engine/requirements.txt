# Universal PDF to DOCX Converter dependencies
# Install: pip install -r requirements.txt
#
# Requires Python 3.11+ (for docling)
# First run of docling downloads models (~2GB) to ~/.cache/docling/

# --- Core engines ---
pdf2docx>=0.5.8          # STANDARD: rule-based PDF to DOCX
PyMuPDF>=1.24.0          # text analysis + page rendering + fallback
docling>=2.0.0           # SCANNED/COMPLEX: IBM multimodal parsing

# --- DOCX rendering (Vision Engine output) ---
python-docx>=1.1.0       # DOCX creation and formatting
beautifulsoup4>=4.12.0   # HTML table parsing (docling output)

# --- Optional ---
# pypandoc>=1.13          # alternative Markdown to DOCX renderer (needs pandoc)
# marker-pdf>=0.3.0       # lighter alternative to docling
